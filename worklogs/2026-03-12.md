# 2026-03-12 — Documentation cleanup and markdownlint stow package

## Documentation Sync

**Priority:** Low | **Type:** Chore

Updated CLAUDE.md and README.md to reflect the current state of the repo (several packages and config details had drifted out of sync with reality).

**Files modified:**
- `CLAUDE.md` — Added `wezterm/` and `claude/` to directory structure; fixed `install.sh` options (`--pwsh`/`--packages` removed, `--system` added); updated Neovim file explorer from Neo-tree → Snacks explorer; added `wezterm` to available stow packages list
- `README.md` — Added `vscode/`, `windowsterminal/`, `wezterm/`, `claude/` to directory structure; fixed `install.sh` usage examples; added `wezterm` row to packages table

## WezTerm Docs Line Reference Update

**Priority:** Low | **Type:** Chore

Updated line number references in wezterm docs to match current `wezterm.lua` layout.

**Files modified:**
- `wezterm/INSTALL.md` — Updated line refs for key bindings (143-181 → 200-252), font (88 → 97), launch menu (187-211 → 254-283)
- `wezterm/README.md` — Updated font size note (10pt → 11pt); updated customization line refs to match

## WORKLOG.md Cleanup

Formatting fixes (blank lines, table alignment) and added Active Tasks section with Prettier Config task.

**Files modified:**
- `WORKLOG.md`

## markdownlint Stow Package

**Priority:** Low | **Type:** New package

Created a `markdownlint` stow package with project-level `.markdownlint.json` config.

**Files created:**
- `markdownlint/.markdownlint.json` — Disables MD013 (line length), MD033 (inline HTML), MD041 (first heading), MD024 (duplicate headings), MD007 (unordered list indent), MD029 (ordered list prefix), MD036 (emphasis as heading)

## Session Complete
